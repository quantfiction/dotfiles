# Sensitive files - never commit these
*_history
*.pem
*.key

# SSH private keys (public keys are OK)
ssh/id_*
!ssh/id_*.pub
ssh/known_hosts
ssh/authorized_keys

# Old SSH key formats (all variants)
*_rsa
!*_rsa.pub
*_dsa
!*_dsa.pub
*_ecdsa
!*_ecdsa.pub
*_ed25519
!*_ed25519.pub

# Environment files with secrets
.env
.env.*
*credentials.json
*secrets*

# Backup files created by install script
*.backup.*

# OS files
.DS_Store
Thumbs.db
node_modules/
auth.json
