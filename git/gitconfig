# NO DEFAULT USER - Set per-repo to avoid committing with wrong account
# Use: git personal  OR  git work  in each repo

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

[init]
	defaultBranch = main

[core]
	editor = nano

[pull]
	rebase = false

[push]
	default = current

[alias]
	# Set work identity in current repo
	work = "!git config user.name 'Brian Blandin' && git config user.email 'brian@thetie.io' && echo 'Switched to work identity'"

	# Set personal identity in current repo
	personal = "!git config user.name 'Quant Fiction' && git config user.email 'quantfiction1@protonmail.com' && echo 'Switched to personal identity'"

	# Check which identity is active (will error if not set)
	whoami = "!git config user.name && git config user.email"

	# Common shortcuts
	st = status
	co = checkout
	br = branch
	ci = commit
	unstage = reset HEAD --
[safe]
	directory = /home/ubuntu/actions-runner/_work/mindhive/mindhive
	directory = /home/ubuntu/actions-runner/_work/mindhive/mindhive
